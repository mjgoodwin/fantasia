.header
  %h1
    #{@model.name}
    -# - if @league_op.policy.(:edit?)
    -#   - # TODO: test links with admin, owner, non-owner.
    = link_to "Edit", edit_league_path(@model)
    -# - if @league_op.policy.(:delete?)
    -#   = link_to "Delete", league_path(@model), method: :delete

.commissioner
  Commish: #{@model.commissioner.email}
